{
  "name": "hl-agents",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@8.15.6",
  "keywords": ["pi-package"],
  "pi": {
    "extensions": ["./packages/pi-review-loop/src/index.ts"]
  },
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "tsup": "^8.3.0",
    "vitest": "^2.1.0"
  }
}